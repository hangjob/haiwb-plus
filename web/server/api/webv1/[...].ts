// import {readyHandler} from "ioredis/built/redis/event_handler";

// const baseURL = process.server ? 'http://172.16.14.191:3010' : '';
export default defineEventHandler(async (event) => {
    // const headers = event.node.req.headers || {};
    // const body = await readBody(event)
    // const query = getQuery(event)
    // console.log(baseURL + event.node.req.url)
    // return await $fetch(baseURL + event.node.req.url, {
    //     method: 'POST',
    //     body,
    //     query,
    //     // @ts-ignore
    //     headers: {
    //         "tag": 'nuxt',
    //         ...headers
    //     },
    // })

    // const config = useRuntimeConfig(event)
    // const header  = await readyHandler(event)
    // const body = await readBody(event)
    // console.log(header,3333);
    // return await $fetch(baseURL + event.node.req.url, {
    //     headers: {
    //         tag: `${config.tag}`
    //     }
    // })
})
